# Đọc trước

Gói này dành cho người chạy cell. Mọi file nhắc tới nằm cùng thư mục.

**Đọc:** `HUONG_DAN_THI_NGHIEM_CELL_THAT.md`, 9 mục ngắn. Làm theo thứ tự; việc đầu tiên là mục 2,
tám kiểm tra của ngày đầu, gửi tám dòng số về rồi mới sang Pha 2.

**In:**

| File | Khổ | Cách in | Kiểm sau khi in |
|---|---|---|---|
| `charuco_a3_o45mm.pdf` | A3 ngang | **100% / actual size**, giấy mặt mờ | đo vạch 100 mm in sẵn; sai là in lại |
| `position_cards.pdf` | A4 dọc, 2 trang | **100% / actual size** | đo vạch 50 mm in sẵn, cắt 21 thẻ |
| `ban_ve_ga_ban_co.pdf`, `ban_ve_ga_3d.pdf`, `ban_ve_toa_do_the.pdf` | A4 ngang | in thường | bản vẽ để đọc |

Chỉ dùng tấm bàn cờ A3 ô 45 mm. Tấm đã in và đã hiệu chuẩn ngày 18/09; chỉ in lại khi phải hiệu
chuẩn lại.

**Gửi về sau mỗi buổi:** thư mục `results\<tên buổi>\`, `logs\experiment.log`, thư mục khung ảnh
trên ổ D, file khoá `resultslinding_keys\key_<buổi>.json` **chưa mở**, trang nhật ký.
